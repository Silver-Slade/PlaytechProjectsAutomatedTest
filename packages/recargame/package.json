{
  "name": "@qa/recargame",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "playwright test",
    "test:admin": "playwright test --project=admin",
    "test:seller": "playwright test --project=seller",
    "report": "playwright show-report playwright-report",
    "clean:report": "rimraf playwright-report test-results"
  },
  "dependencies": {
    "@qa/framework": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.0.0",
    "dotenv-cli": "^7.4.0",
    "rimraf": "^5.0.0"
  }
}
